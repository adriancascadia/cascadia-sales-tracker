import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cascadiasalestracker.app',
  appName: 'Cascadia Sales Tracker',
  webDir: 'dist/public'
};

export default config;
